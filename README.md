# electricity-demand-forecasting

1. Create estimator for `estimator = LGBMRegressor(random_state=15926, verbose=-1)` and test without exogenous features
2. Test with exogenous features
3. Use `tuning.py` for hyperparameter tuning
4. Used tuned hyperparameters and test again with exogenous features
5. Test with setting `refit=True`

TODO:
- write explain.py